function combsbd(outputfilename, inputfilename1, inputfilename2)
%COMBSBD    Combines data from Spacar Binary Data (SBD) files into one file.
%
%   COMBSBD(OUTPUTFILENAME, INPUTFILENAME(S)) combines all data stored in
%   the specified Spacar Binary Data files and creates or overwrites the 
%   output file.
%
%   All FILENAME should specify the file name WITHOUT the extension .SBD.
%
%   See also CHECKSBF, GETFRSBF, SPADRAW.

% Note that this is only a help message as a MEX function will 
% actually be executed.

% Rev. 30-Aug-2002 R.Aarts.
